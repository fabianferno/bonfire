# MongoDB
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=bonfire

# Auth — JWT_SECRET must be at least 32 chars; boot fails otherwise
JWT_SECRET=replace-me-with-a-long-random-secret-at-least-32-chars
JWT_EXPIRES_IN=7d

# Server
PORT=8080
LOG_LEVEL=info
